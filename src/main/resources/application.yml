spring:
  application:
    name: order
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    password: password
    username: root
    url: jdbc:mysql://127.0.0.1:3306/springcloud_self?characterEncoding=utf-8&useSSL=false&serverTimezone=GMT%2B8

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
